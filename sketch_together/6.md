After copying the code snippet above, head back to Firebase and select the `Authentication` tab. Select `SIGN-IN METHOD` from the top menu bar, and click `ADD DOMAIN`. Enter `repl.co` and click `ADD`.

![](img/auth.png)